// This is declartion of interfaces to be used by array sample code
class MyArray
{
    private:
        int arrayValue[128];

    public:
        MyArray();
        int addValue(int);
        int printValue(int);
};
